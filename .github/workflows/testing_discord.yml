name: Testing discord

on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]
    types: [opened, synchronize]

jobs:
  simple_deployment_pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: npm ci
      - name: Check style
        run: npm run eslint
      - name: Test Failure
        if: ${{ failure() }}
        id: failure
        run: |
          error_path=$(curl https://api.github.com/repos/thomastoumasu/full-stack-open-pokedex/check-runs/53849423899/annotations | jq '.[-1].path')
          error_message=$(curl https://api.github.com/repos/thomastoumasu/full-stack-open-pokedex/check-runs/53849423899/annotations | jq '.[-1].message')
          echo "error_path=$error_path" >> $GITHUB_OUTPUT
          echo "error_message=$error_message" >> $GITHUB_OUTPUT
          echo "global_error_path=$error_path" >> $GITHUB_ENV
          echo "global_error_message=$error_message" >> $GITHUB_ENV
          echo "$error_path"
          echo "$error_message"
  post_on_discord:
    if: ${{ always() }}
    needs: [simple_deployment_pipeline]
    runs-on: ubuntu-latest
    steps:
      - name: echo error message
        run: |
          echo "global error path $global_error_path"
          echo "global error message $global_error_message"
          echo "job error path ${{ needs.simple_deployment_pipeline.steps.failure.outputs.error_path }}"
          echo "job error message ${{ needs.simple_deployment_pipeline.steps.failure.outputs.error_message }}"
